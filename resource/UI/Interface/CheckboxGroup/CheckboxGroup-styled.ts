import styled from "styled-components";

export const CheckboxGroupElement = styled.div
`
    margin: 0 0 10px;
    padding: 0;
    display: flex;
    position: relative;
    width: 201px;
    height: auto;
    z-index: 0;
    overflow: visible;
    flex-direction: row;
`;
